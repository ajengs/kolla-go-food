<h1>1. The number of order every date</h1>
<%= line_chart @number_of_orders %>

<h1>2. The total price of order every date</h1>
<%= line_chart @total_price_per_date%>

<h1>3. The number of order for each food</h1>
<%= column_chart @number_of_orders_per_food %>

<h1>4. The total price of order for each food</h1>
<%= column_chart @total_price_per_food %>

<h1>5. The number of order from each restaurant</h1>
<%= column_chart @number_of_orders_per_restaurant %>

<h1>6. The total price of order from each restaurant</h1>
<%= column_chart @total_price_per_restaurant %>
